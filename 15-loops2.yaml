- name: "install packages"
  hosts: "frontend"
  become: yes
  tasks:
    - name: "package installation"
      ansible.builtin.package:
        name: "{{ item }}"
        state: "present"
      loop:
      - nginx
      - mysql
      - zip