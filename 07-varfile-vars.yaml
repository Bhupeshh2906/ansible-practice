- name: "play level variables"
  hosts: "localhost"
  vars_files:
  - vars_files
  tasks:
    - name: " print course information"
      ansible.builtin.debug:
        msg: "Hello {{ course }}, the course duration is {{ duration }} and the trainer is {{ trainer }}."